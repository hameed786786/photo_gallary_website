Photo Management App

Overview

React app to upload and view photos with Firebase Storage integration. Images are stored in Firebase Cloud Storage and metadata is stored in Firestore, eliminating localStorage limitations.

Requirements

- Node.js and npm


powershell
npm install


Run the app:
powershell
npm start

